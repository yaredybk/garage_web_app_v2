export default function BreakLine({ children }) {
    return (
        <div className=" flex-shrink-0 flex-grow w-full  border-solid border-0 border-t-2  border-slate-300">
            {children}
        </div>
    );
}
